"""Git evidence + safety invariants. v0.1 build-step-1 provides only the minimal
``repositoryIdentity`` slice that ``init`` needs; the full evidence collector
(changesets, pinned diff policy) is build-step-5.
"""
