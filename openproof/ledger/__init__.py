"""The ``.openproof/`` store + import lifecycle.

v0.1 is pure files + a fresh per-invocation index. The redacted projection lives in
gitignored ``raw/`` (the citation target); originals live in gitignored ``vault/``.
"""
